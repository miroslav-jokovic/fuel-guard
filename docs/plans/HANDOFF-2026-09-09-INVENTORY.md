# Handoff — shop inventory, 2026-09-09

For whoever picks up **I4**. Written at the end of the session that built I2, I3 and the review of
I0–I3.

The plan is `docs/plans/maintenance/INVENTORY-PLAN.md` and it is the memory between sessions — §8 is
a dated progress log with every deviation and every measurement in it. **Read §8's last four entries
before writing anything**; this file is the map, not a replacement for them.

---

## Where it stands

`main` at `53fc7b2` carries **I0–I3**. One PR is open.

| Step | State | PR |
|---|---|---|
| I0 governance | merged | #684 |
| I1 contracts + `inventoryRules` | merged | #685 |
| I1b `@silvicom/qr` | merged | #686 |
| I9's prerequisite `listEquipmentIdentities` (out of order) | merged | #688 / #689 |
| **I2** schema + service — migration 0331 | merged | #691 |
| **I3** the API, mounted | merged | #692 |
| **Review of I0–I3** — five defects + one stale doc | **OPEN** | **#693** |
| **I4** the shop home and Parts | **next** | — |

**Merge #693 before starting I4.** It is not tidying: three of its five findings are fields the
product stored and could never display, and I4's screens are what would have displayed them.

## What exists to build on

- **Four tables** (0331): `stock_locations`, `parts`, `part_stock`, `part_movements`. The quantity is
  a projection of the ledger and `record_part_movement` is its only writer.
- **The API**, mounted at `/api/maintenance/inventory/…`: `parts` (list · `by-upc/:upc` · one ·
  create · patch · `:id/photo`), `locations` (list · create · patch), `stock`, `low-stock`,
  `movements`, `PATCH /stock/:partId/:locationId`, and the six verbs `receive` · `issue` · `adjust` ·
  `transfer` · `return` · `count`.
- **The contracts** (`packages/shared/src/inventoryContract.ts` and friends) — DTOs, the six movement
  payloads as a discriminated union, `isLowStock`, `countVarianceTier`, `deriveKitStatus`.
- **`@silvicom/qr`** — encoding and label-sheet geometry, unused until I10.

## The five things worth knowing before you touch it

1. **This shop has no shelf numbers** (owner, 2026-09-09, §1.4). `aisle`/`row`/`bin` are optional and
   nothing requires them. **Do not lead a Parts form with them.** What addresses a shelf here is the
   label — `tag_code` and D-INV7's resolver, which arrive at I6/I10.
2. **A movement id is generated by the CLIENT** and is the idempotency key (D-INV27). The web form
   must mint one UUID **per movement**, not per attempt — a retry sends the same id and gets the same
   row back with a 201. Minting per attempt breaks the offline queue's server half and breaks no test.
3. **`/low-stock` is deliberately unpaginated.** It returns the whole list because a low-stock screen
   that under-reports says "nothing to order" and is believed. Do not add `limit`/`offset` to it.
4. **Errors carry a code and a status that mean something.** 409 = the shelf refuses a fine payload
   (`IV010` not enough, `IV011` ledger not editable, `IV016` retry). 422 = the payload names something
   unusable (`IV012`/`IV013`/`IV014`/`IV015`). The screens should render the message, not "failed".
5. **`lint:filesize` covers `.vue`** — measured at I0, 500 hard / 450 warn. Extract the forms from the
   first commit rather than after the gate fails.

## What I4 needs from the owner — A3, and it is the only blocker

> **A3** — an initial parts list exists to import (a spreadsheet or a FleetPal export).
> Retired by: **before I4** — the file and its columns.

Unanswered. The plan's default is a locked-header CSV template with an error report, which is
buildable without the file — but if a real list exists, its columns should shape the importer rather
than the shop reshaping its data to fit ours. **Ask for it first; build the template only if there
isn't one.**

The rest of I4 — the shop home, the Parts list and detail, the nav group — is unblocked.

## The other assumptions, and which step retires each

`A1` the free decoder on the shop's phones → **I6 spike**. `A2` the shop's phones and bay wifi → I6
(its shelving half is answered; see §8's I2 entry for why it never gated I2). `A4` kit contents per
unit kind → I9. `A5` the label printer and stock → I10. `A6` FleetPal's export path → Q1, not
blocking. `A7` `uqr` determinism → **partly** retired by I1b's golden fixtures; its row stands until
I10 proves a real scanner reads a real printed label.

## Traps this programme has already paid for

- **A comment claiming a hazard that measurement denies is worse than no comment.** `/by-upc/:upc`
  carried one about route ordering; moving the route failed no test, because two path segments cannot
  collide with one. Mutate before you warn.
- **`supabaseRecorder` does not filter** — `.eq()` is recorded, not applied. Use function fixtures or
  `pages`, or a flat array will answer two different questions with the same rows.
- **zod STRIPS unknown keys** rather than rejecting them. To prove a field cannot reach the database,
  assert on what was written, not on a 400.
- **A fresh worktree needs `pnpm install` + `build:rn` + two `.env` files**, or ~14 suites fail for
  reasons that are not your change.
- **Run all 38 `lint:*` scripts before pushing**, not the 28 CI names — `lint:schema-snapshot` and
  three others are chained onto neighbours.

## Still open elsewhere, not inventory's problem but worth knowing

- The **api test flake** — one unrelated test per full local run, six hypotheses ruled out in #690,
  root cause open. If it hits you, it is not your change.
- The **BOL reading-reliability programme** (#680, merged) is unstarted and begins at Phase 0
  measurement.
