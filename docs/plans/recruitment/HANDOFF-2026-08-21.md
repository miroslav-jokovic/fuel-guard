# Handoff · 2026-08-21 — start building the application system (A1 first)

Deliberately short. The plans are the memory; this page only tells a fresh session where to
stand and what to open first.

## 1. What happened today

- **`APPLICATION-SYSTEM-PLAN.md` landed on main** (PR #149, merge `e47e21a`) together with its
  R2b cross-reference in `RECRUITING-SYSTEM-PLAN.md`. Both were re-verified the same day by a
  three-track audit (every §0/§3 claim against the tree at `75020bf`; regulatory claims against
  psp.fmcsa.dot.gov / clearinghouse.fmcsa.dot.gov / ecfr.gov). All corrections are folded into
  the plans in place, marked ⚠ — **do not re-derive them, and do not trust any earlier chat's
  memory of these documents over the documents.**
- The notable corrections, so you recognise them when you meet them: the PSP instrument's v1
  text is FMCSA's mandated form, not counsel's (A0); §391.21(b)(4) has no contract field yet
  (A3 closes it); a resumed draft needs a DOB unlock, not just the link (new D-APP16);
  `submit_driver_application`'s live body is **0222's**, not 0220's; `startDqAlertScheduler`
  is 6-hourly, not daily.

## 2. Where to stand

1. Run `RECRUITING-SYSTEM-PLAN.md` **§4's resume ritual** — it is the protocol for every
   session, unchanged. Read that plan, then `APPLICATION-SYSTEM-PLAN.md` top to bottom, then
   the `CLAUDE.md` of every package the step touches.
2. `git log --oneline -15` and `pnpm verify:live` before believing anything about deploy state.
3. Nothing of A0–A11 is built. Migration head at handoff: `0224`. Never pin migration numbers —
   next-numbered at execution.

## 3. The next step is A1 — the invitation becomes a resumable session

It fixes the live defect (`APPLICATION-SYSTEM-PLAN.md` §0.2: `used_at` kills the link that the
post-submit copy promises will still work) and everything else stacks on it. One branch
(`claude/<topic>`), PR to `main`, merge after CI — then mark A1 **DONE** in the plan with
"What shipped" and "Verified by:", as §5's register requires. Then A2 (drafts + autosave +
D-APP16's unlock), then A3, in the plan's order.

## 4. Clocks that run outside the code (owner-facing; raise them, don't wait on them)

- **Counsel wording (Q-H3 / A0):** four instruments to draft + the 7001(c) consent; the PSP
  one is transcription of FMCSA's form. Nothing blocks building — every ceremony step verifies
  against a non-draft fixture.
- **10DLC brand/campaign registration** (D-APP13): the plan says it opens the day A1 opens.
  That day is now.
- **The owner's Excel application** (A9's input) — chase it early, it blocks only A9.
- **Clearinghouse owner acts** (R5, later): query-plan purchase is employer-only; from
  2026-04-27 registration needs IDEMIA identity verification.

## 5. Standing cautions (verified today, easy to re-break)

- QA happens in the `FuelGuard EFS QA` org (`07fe4058-…`, null `dot_number`) — never Silvicom.
- The public apply surface has **two stacked rate limiters** (`app.ts:147` and `:231`); A2's
  autosave budget is the intersection.
- One local tone record survives in `ApplicationInviteCard.vue:74` (`STATE_TONE`) — fold its
  removal into the first UI-touching PR.
- `docs/McLeod-Testing/` sits untracked in the working tree at handoff — not this work's;
  leave it.
