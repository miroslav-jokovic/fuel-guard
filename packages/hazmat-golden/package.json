{
  "name": "@hazmat/golden",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HazmatGuard golden acceptance suite — SME-authored scenarios run through the real @hazmat/engine + @hazmat/data.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "golden": "tsx src/cli.ts"
  },
  "dependencies": {
    "@hazmat/engine": "workspace:*",
    "@hazmat/data": "workspace:*",
    "zod": "4.4.3",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
