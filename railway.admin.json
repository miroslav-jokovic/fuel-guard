{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile --prod=false && rm -rf apps/admin/dist && pnpm --filter @fuelguard/admin build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @fuelguard/admin-api start",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
